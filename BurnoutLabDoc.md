# Документация проекта Burnout Lab v3

Это одностраничное веб-приложение (Landing Page) для ретрита "Burnout Lab" в Японии. Приложение построено на **React**, использует **Tailwind CSS** для стилизации и включает интеграции с **Telegram** (для заявок) и **Google Gemini** (голосовой ассистент).

## 🛠 Технический стек

*   **Фреймворк:** React 19
*   **Язык:** TypeScript
*   **Стилизация:** Tailwind CSS (подключен через CDN в `index.html` с кастомной конфигурацией)
*   **Иконки:** Lucide React
*   **AI:** Google GenAI SDK (Gemini 2.5 Flash Native Audio)

---

## ⚙️ Конфигурация (`config.ts`)

Все основные настройки вынесены в файл `config.ts`. Это единое место для управления ссылками, токенами и текстами.

### 1. Настройки Telegram (`TG_CONFIG`)
Используются для отправки данных из формы бронирования ("Apply Now") в ваш Telegram канал или группу.

```typescript
export const TG_CONFIG = {
  TOKEN: "ВАШ_БОТ_ТОКЕН",      // Токен от @BotFather
  CHAT_ID: "-100xxxxxxxxx",    // ID канала или группы (начинается с -100 для супергрупп)
  THREAD_ID: 5                 // (Опционально) ID темы/треда, если включены темы в группе
};
```

### 2. Ресурсы (`ASSETS`)
Ссылки на медиа-файлы (видео, изображения, PDF).

```typescript
export const ASSETS = {
  PDF: "ссылка_на_pdf_файл",
  VIDEO_BG: "ссылка_на_видео_для_фона_десктоп",
  VIDEO_MOBILE: "ссылка_на_видео_для_фона_мобильный",
  FILM_VIDEO: "ссылка_на_полное_видео_для_модального_окна",
  ABOUT_IMG: "ссылка_на_фото_в_секции_about",
  COACHES: [ ...массив_ссылок_на_фото_коучей... ]
};
```

---

## 📁 Структура файлов

*   **`index.html`** — Входная точка HTML.
    *   Здесь подключен Tailwind CSS через CDN.
    *   Здесь находится конфигурация темы Tailwind (`tailwind.config`), включая кастомные шрифты, цвета (`brand`) и анимации (`float`, `draw`, `pulse-glow`).
*   **`index.tsx`** — Точка входа React приложения. Монтирует `App` в DOM.
*   **`App.tsx`** — Основной код приложения. Содержит:
    *   Всю верстку (Hero, About, Schedule, Team, Pricing, Footer).
    *   Логику модальных окон.
    *   Логику голосового ассистента.
    *   Текстовые данные (`TRANSLATIONS`).
*   **`config.ts`** — Файл конфигурации (см. выше).

---

## 🚀 Основные функции

### 1. Форма заявки и Telegram
При отправке формы в модальном окне:
1.  Генерируется уникальный `Ref` номер (например, `REF-A1B2C3`).
2.  Скрипт собирает данные формы + UTM метки из URL + параметр `?from`.
3.  Данные отправляются через Telegram Bot API (`sendMessage`) в указанный чат.
4.  При успехе показывается анимация галочки и номер заявки.

**Параметры URL, которые отслеживаются:**
*   `from`
*   `utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`

### 2. Голосовой ассистент (AI)
В правом нижнем углу находится кнопка (иконка искр/микрофона).
*   **Модель:** `gemini-2.5-flash-native-audio-preview-12-2025`.
*   **Функция:** Позволяет пользователю задать вопрос голосом и получить мгновенный голосовой ответ.
*   **Требование:** Для работы необходим `API_KEY` в переменных окружения (`process.env.API_KEY`).

### 3. Анимации
Используется `IntersectionObserver` (компонент `<Reveal>`) для анимации элементов при прокрутке.
*   Тексты выплывают снизу (`fade-in-up`).
*   Параллакс эффект на фотографиях команды.
*   Эффект "рисования" (`draw`) галочки при успешной отправке формы.

---

## 🎨 Кастомизация

### Изменение текстов
Все тексты находятся внутри объекта `TRANSLATIONS` в файле `App.tsx`. Вы можете редактировать их там.

### Изменение цветов
Цветовая палитра настроена в `index.html` в секции `tailwind.config`. Основной цвет — `brand` (оттенки красного/розового).
Чтобы изменить основной цвет, поменяйте HEX-коды в объекте `colors.brand`.

### Добавление разрешений
Если приложению понадобятся камера или геолокация, добавьте их в `metadata.json`. Сейчас там только `microphone` для работы AI ассистента.

```json
{
  "requestFramePermissions": [
    "microphone"
  ]
}
```